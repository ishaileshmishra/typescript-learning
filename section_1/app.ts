// it says all about the requiremeny of the typescript and how it removes the errorious code blocks
function add(n1: number, n2: number) {
  return n1 + n2;
}

const number1 = 5;
const number2 = 6;

console.log(typeof number1);
const result = add(number1, number2);
console.log("result is: " + result);
